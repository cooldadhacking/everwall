[package]
name = "everwall"
version = "0.1.0"
edition = "2024"
description = "Recolor any wallpaper into the Everforest palette"
license = "MIT"

[dependencies]
image = "0.25"
clap = { version = "4", features = ["derive"] }
rayon = "1"
anyhow = "1"

[profile.release]
opt-level = 3
lto = "thin"
